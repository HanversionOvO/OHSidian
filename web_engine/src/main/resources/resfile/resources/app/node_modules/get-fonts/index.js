'use strict';

var binding = null;
try {
  binding = require('./binding.node');
} catch (error) {
  console.warn('[HMOS-COMPAT] get-fonts native addon unavailable, using empty font list fallback:', error.message);
}

module.exports = {
  getFonts: () => binding ? binding.getFonts() : []
}
